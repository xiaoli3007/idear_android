<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/background_highlight" >

    <LinearLayout
        android:id="@id/order_record_listitem_content_layout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:orientation="vertical"
        android:paddingBottom="10.0dip"
        android:paddingTop="10.0dip" >

        <TextView
            android:id="@id/order_record_listitem_title_text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:paddingLeft="20.0dip"
            android:singleLine="true"
            android:text="@string/app_name"
            android:textSize="16.0sp" />

        <TextView
            android:id="@id/order_record_listitem_desc_text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:paddingLeft="20.0dip"
            android:singleLine="true"
            android:text="@string/app_name"
            android:textColor="@color/text_gray"
            android:textSize="14.0sp" />
    </LinearLayout>

    <TextView
        android:id="@id/order_record_listitem_status_text"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:ellipsize="end"
        android:gravity="right|center_vertical"
        android:paddingRight="20.0dip"
        android:singleLine="true"
        android:text="@string/app_name"
        android:textSize="16.0sp" />

    <Button
        android:id="@id/order_record_listitem_show_detail_button"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignBottom="@id/order_record_listitem_content_layout"
        android:layout_alignLeft="@id/order_record_listitem_content_layout"
        android:layout_alignRight="@id/order_record_listitem_status_text"
        android:layout_alignTop="@id/order_record_listitem_content_layout"
        android:layout_centerVertical="true"
        android:background="#00000000" />

</RelativeLayout>